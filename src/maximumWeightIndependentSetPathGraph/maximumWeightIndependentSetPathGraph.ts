/**
 * Given a connected path graph with vertex weights
 * return the maximum weight of an independent set of vertices
 */
// @ts-ignore
export function maximumWeightIndependentSetPathGraph(graph: number[]): number {
  if (!graph.length) return 0;

}
